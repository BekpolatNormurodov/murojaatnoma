org.gradle.jvmargs=-Xmx768m -XX:MaxMetaspaceSize=384m -XX:+HeapDumpOnOutOfMemoryError
org.gradle.workers.max=2
android.useAndroidX=true
# tflite_flutter compiles Java at target 1.8 but Kotlin at 21; downgrade
# Gradle's JVM-target consistency check from error -> warning so the release
# build succeeds (same fix already in worker-app).
kotlin.jvm.target.validation.mode=warning
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
